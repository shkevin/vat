# VAT Backend — Python 3.11+
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Auth
python-jose[cryptography]==3.5.0
httpx>=0.27.0

# Task queue (sync worker)
celery[redis]>=5.3.0
redis>=5.0.0

# Excel export for Aikido sync validation (data science / leadership reporting)
openpyxl>=3.1.0
pandas>=2.0.0

# XML parsing (OpenSCAP XCCDF) — defusedxml for security
defusedxml>=0.7.0

# OpenTelemetry (operational mirror for audit events)
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
opentelemetry-exporter-otlp>=1.28.0
