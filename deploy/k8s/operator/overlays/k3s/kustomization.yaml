apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
components:
  - ../../components/node-scanning
patches:
  - path: patch-runtime-profile.yaml
# Operator intentionally stopped (scaled to 0) — 2026-07-13. Revert to resume.
replicas:
  - name: vat-operator
    count: 0
