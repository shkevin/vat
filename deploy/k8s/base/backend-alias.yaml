apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: vat
  labels:
    app.kubernetes.io/name: vat-backend
spec:
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app.kubernetes.io/name: vat-backend
